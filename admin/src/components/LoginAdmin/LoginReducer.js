export const initialState = {
    email: '',
    password: '',
    token: '',
    isLogin: false,
    isRightPassword: '',
};

const LoginReducer = (state = initialState, action) => {
    return state;
};

export default LoginReducer;
